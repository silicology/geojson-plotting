# geojson-plotting

Scaling and Centering: Adjusted the coordinates by scaling (multiplying by 100) and centering (adding half of the SVG size to both x and y coordinates) in draw_polygon, draw_point, and draw_linestring functions.
